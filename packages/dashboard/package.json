{
  "name": "@featuresync/dashboard",
  "version": "0.0.0",
  "description": "Locally-run dashboard to browse, publish and roll back FeatureSync snapshots.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "featuresync-dashboard": "dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node -e \"for (const dir of ['styles', 'scripts']) require('node:fs').cpSync('src/infrastructure/views/' + dir, 'dist/infrastructure/views/' + dir, { recursive: true })\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@featuresync/aws": "workspace:*",
    "@featuresync/core": "workspace:*"
  },
  "peerDependencies": {
    "@aws-sdk/client-s3": "^3.1135.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1135.0",
    "@playwright/test": "^1.63.0"
  }
}
