{
  "name": "featuresync",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run --coverage",
    "test:integration": "pnpm --filter @featuresync/core build && pnpm --filter @featuresync/aws build && pnpm --filter @featuresync/cli build && pnpm --filter @featuresync/aws test:integration",
    "verify": "pnpm --filter @featuresync/core build && pnpm --filter @featuresync/aws build && pnpm --filter @featuresync/cli build && pnpm typecheck && pnpm lint && pnpm test",
    "example:node-local": "pnpm --filter @featuresync/core build && pnpm --filter @featuresync/example-node-local start"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.6.1",
    "@vitest/coverage-v8": "^5.0.1",
    "eslint": "^10.10.0",
    "eslint-import-resolver-typescript": "^4.4.5",
    "eslint-plugin-import-x": "^4.17.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.70.0",
    "vitest": "^5.0.1"
  }
}
